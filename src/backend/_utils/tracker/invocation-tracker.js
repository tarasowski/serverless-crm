module.exports = (userId, companyId, resourceName, namespace) => {
    console.log(`MONITORING|{"userId": "${userId}", "companyId": "${companyId}", "resourceName": "${resourceName}", "namespace": "${namespace}"}|`)
}
